import React from "react";
import PropTypes from "prop-types";
const Code = () => (
  <div className={`font-bold text-6xl md:text-8xl xl:text-9xl`}>
    <code>!==</code>
  </div>
);

Code.propTypes = {};

export default Code;
